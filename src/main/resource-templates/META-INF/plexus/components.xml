<component-set>
    <components>
        <component>
            <role>
                org.apache.maven.lifecycle.mapping.LifecycleMapping
            </role>
            <role-hint>nuget</role-hint>
            <implementation>
                org.apache.maven.lifecycle.mapping.DefaultLifecycleMapping
            </implementation>
            <configuration>
                <lifecycles>
                    <lifecycle>
                        <id>default</id>
                        <phases>
                            <initialize>${project.groupId}:${project.artifactId}:${project.version}:initialize</initialize>
                            <compile>${project.groupId}:${project.artifactId}:${project.version}:build</compile>
                            <test>${project.groupId}:${project.artifactId}:${project.version}:test</test>
                            <package>
                                ${project.groupId}:${project.artifactId}:${project.version}:pack
                            </package>
                            <install>${project.groupId}:${project.artifactId}:${project.version}:install</install>
                            <deploy>${project.groupId}:${project.artifactId}:${project.version}:push</deploy>
                        </phases>
                    </lifecycle>
                    <lifecycle>
                        <id>clean</id>
                        <phases>
                            <clean>
                                org.apache.maven.plugins:maven-clean-plugin:${maven-clean-plugin.version}:clean,
                                ${project.groupId}:${project.artifactId}:${project.version}:clean
                            </clean>
                        </phases>
                    </lifecycle>
                </lifecycles>
            </configuration>
        </component>
        <component>
            <role>org.apache.maven.artifact.handler.ArtifactHandler</role>
            <role-hint>nuget</role-hint>
            <implementation>
                org.apache.maven.artifact.handler.DefaultArtifactHandler
            </implementation>
            <configuration>
                <type>nuget</type>
                <extension>nupkg</extension>
                <language>c#</language>
                <addedToClasspath>false</addedToClasspath>
            </configuration>
        </component>
        <component>
            <role>org.sonatype.plexus.components.sec.dispatcher.SecDispatcher</role>
            <role-hint>dotnet-security</role-hint>
            <implementation>org.sonatype.plexus.components.sec.dispatcher.DefaultSecDispatcher</implementation>
            <requirements>
                <requirement>
                    <role>org.sonatype.plexus.components.cipher.PlexusCipher</role>
                    <role-hint>dotnet-security</role-hint>
                    <field-name>_cipher</field-name>
                </requirement>
            </requirements>
            <configuration>
                <_configuration-file>~/.m2/settings-security.xml</_configuration-file>
            </configuration>
        </component>
        <component>
            <role>org.sonatype.plexus.components.cipher.PlexusCipher</role>
            <role-hint>dotnet-security</role-hint>
            <implementation>org.sonatype.plexus.components.cipher.DefaultPlexusCipher</implementation>
        </component>
    </components>
</component-set>